function [Y] = myLoop()
    %%%%%%%
    % DOMAIN:   the set of results from the previous
    %           process.
    % SELECTS:  
    %%%%%%%
    
    
    
    looks to the set of results from the previous
    % process.calls the next process for
    % each of the 
    
    for i = 1:numel(X)
        Y = X{i};
    end
end